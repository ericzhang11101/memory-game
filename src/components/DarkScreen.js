import React from 'react'

export default function DarkScreen(props) {
    const {onPress} = props
    return (
        <div class='dark-screen' id='darkScreen' onClick={onPress}>
            
        </div>
    )
}
